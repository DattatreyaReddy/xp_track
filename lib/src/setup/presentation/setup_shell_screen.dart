import 'package:flutter/material.dart';

class SetupShellScreen extends StatelessWidget {
  const SetupShellScreen({super.key, required this.child});
  final Widget child;
  @override
  Widget build(BuildContext context) {
    return child;
  }
}
